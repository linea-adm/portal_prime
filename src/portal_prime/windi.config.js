import { defineConfig } from 'windicss/helpers';

export default defineConfig({
  // Adicione ou configure suas opções aqui
  // Exemplo de configuração básica:
  theme: {
    extend: {},
  },
  plugins: [
    // Adicione plugins do WindiCSS aqui
    // Por exemplo, plugin de espaçamento (space)
  ],
});
